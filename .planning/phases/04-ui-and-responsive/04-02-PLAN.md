---
phase: 04-ui-and-responsive
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/ui/InfoPanel.jsx
  - src/components/ui/NavSidebar.jsx
  - src/App.jsx
  - src/components/ui/TimelineControl.jsx
autonomous: false

must_haves:
  truths:
    - "On desktop (>=768px) the sidebar is pinned on the left and the info panel slides in from the right"
    - "On mobile (<768px) the sidebar is hidden behind a hamburger button and the info panel appears as a bottom sheet from below"
    - "Touching/dragging on NavSidebar, InfoPanel, or TimelineControl does not rotate the 3D scene behind them"
    - "The hamburger button is only visible on mobile (<768px)"
    - "Selecting a planet from the sidebar on mobile closes the sidebar automatically"
  artifacts:
    - path: "src/components/ui/InfoPanel.jsx"
      provides: "Responsive InfoPanel — right sidebar on desktop, bottom sheet on mobile"
      contains: "md:top-0 md:right-0"
    - path: "src/components/ui/NavSidebar.jsx"
      provides: "Responsive NavSidebar — pinned on desktop, hamburger-driven on mobile"
      contains: "md:translate-x-0"
    - path: "src/App.jsx"
      provides: "Hamburger button (md:hidden), isNavOpen state wired to toggle"
      contains: "md:hidden"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/ui/NavSidebar.jsx"
      via: "isNavOpen state + setIsNavOpen toggle hamburger"
      pattern: "setIsNavOpen"
    - from: "src/components/ui/InfoPanel.jsx"
      to: "OrbitControls (Canvas)"
      via: "stopPropagation on touch/pointer events prevents canvas interaction"
      pattern: "stopPropagation"
    - from: "src/components/ui/NavSidebar.jsx"
      to: "OrbitControls (Canvas)"
      via: "stopPropagation on touch/pointer events prevents canvas interaction"
      pattern: "stopPropagation"
---

<objective>
Make all UI components responsive (desktop sidebar + panel vs mobile hamburger + bottom sheet) and add touch event isolation so HTML overlay interactions don't rotate the 3D scene.

Purpose: The app must be usable on both desktop and mobile devices. Touch isolation is critical for mobile UX — without it, interacting with UI panels simultaneously rotates the 3D scene behind them.
Output: Updated InfoPanel, NavSidebar, App.jsx, TimelineControl with responsive classes and touch isolation.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-and-responsive/04-RESEARCH.md
@.planning/phases/04-ui-and-responsive/04-01-SUMMARY.md
@src/App.jsx
@src/components/ui/InfoPanel.jsx
@src/components/ui/NavSidebar.jsx
@src/components/ui/TimelineControl.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make NavSidebar and InfoPanel responsive, add hamburger button</name>
  <files>
    src/components/ui/NavSidebar.jsx
    src/components/ui/InfoPanel.jsx
    src/App.jsx
  </files>
  <action>
    **NavSidebar.jsx — Responsive mobile/desktop:**
    - Update the root nav element classes to support both layouts in one class string:
      - Mobile (default): `-translate-x-full` (hidden off-screen left). When `isOpen` is true: `translate-x-0`.
      - Desktop (md: and above): `md:translate-x-0` (always visible, pinned). The `isOpen` prop has no effect on desktop because `md:translate-x-0` overrides the mobile translate.
    - The class string becomes: `fixed top-0 left-0 h-full w-56 bg-black/80 backdrop-blur-sm z-30 pointer-events-auto overflow-y-auto transition-transform duration-300 ${isOpen ? 'translate-x-0' : '-translate-x-full'} md:translate-x-0`
    - Add touch event isolation handlers to the root div: `onTouchStart={e => e.stopPropagation()}`, `onTouchMove={e => e.stopPropagation()}`, `onPointerDown={e => e.stopPropagation()}`.
    - The `onClose` callback (already accepted as prop from 04-01) is called after planet selection to auto-close on mobile. This is already wired in 04-01.

    **InfoPanel.jsx — Responsive: right sidebar on desktop, bottom sheet on mobile:**
    - Replace the current class string on the root div with responsive classes:
      - Mobile (default): `fixed bottom-0 left-0 right-0 h-[60vh]` — bottom sheet that slides up.
      - Desktop (md: and above): `md:top-0 md:right-0 md:bottom-auto md:left-auto md:h-full md:w-80` — right sidebar that slides in.
    - Visibility transform must be responsive:
      - Hidden state: `translate-y-full md:translate-y-0 md:translate-x-full` (mobile slides down, desktop slides right).
      - Visible state: `translate-y-0 md:translate-x-0` (both in view).
    - Keep existing: `bg-black/80 backdrop-blur-sm z-20 pointer-events-auto transition-transform duration-300 ease-in-out flex flex-col`.
    - Add `rounded-t-2xl md:rounded-none` for mobile bottom sheet rounded top corners.
    - Add `min-h-[320px]` for short phones (iPhone SE at 568px would get only 340px at 60vh).
    - Add touch event isolation handlers to the root div: `onTouchStart={e => e.stopPropagation()}`, `onTouchMove={e => e.stopPropagation()}`, `onPointerDown={e => e.stopPropagation()}`.
    - Add a drag handle indicator for mobile at the top of the panel: a small `div` with `w-10 h-1 bg-white/20 rounded-full mx-auto mt-2 mb-1 md:hidden` — visual cue that it's a bottom sheet (non-functional, cosmetic only).

    **App.jsx — Add hamburger button:**
    - Add a hamburger menu button that toggles `isNavOpen`. Position: `fixed top-4 left-4 z-40 md:hidden pointer-events-auto`.
    - The button should show a hamburger icon (three stacked lines, can be done with spans or Unicode ☰). When sidebar is open, show an X icon instead.
    - Style: `w-10 h-10 flex items-center justify-center rounded-lg bg-black/60 backdrop-blur-sm text-white/70 hover:text-white transition-colors`.
    - `md:hidden` ensures it only appears on mobile (below 768px).
    - On desktop, the NavSidebar is always visible via `md:translate-x-0` — the hamburger is not needed.
    - Ensure `isNavOpen` and `setIsNavOpen` are already defined (should be from 04-01). If not, add `const [isNavOpen, setIsNavOpen] = useState(false)`.
    - Adjust the main content area: on desktop (md:), add left padding to account for the pinned sidebar width (`md:pl-56` on the DOM overlay div or equivalently offset the TimelineControl and Footer so they don't overlap the sidebar). The simplest approach: keep TimelineControl and Footer absolutely positioned — they're centered/right-aligned and won't overlap the left sidebar. No padding adjustment needed.
  </action>
  <verify>
    Run `npm run build` — zero errors.
    Open Chrome DevTools, toggle device toolbar:
    - At 375px width (mobile): hamburger visible at top-left, sidebar hidden, clicking hamburger shows sidebar sliding in, clicking a planet closes sidebar, InfoPanel appears as bottom sheet from below.
    - At 1024px width (desktop): no hamburger visible, sidebar pinned on left, InfoPanel slides from right.
  </verify>
  <done>
    NavSidebar is responsive — pinned on desktop, hamburger-driven on mobile with auto-close on selection. InfoPanel is responsive — right sidebar on desktop, bottom sheet on mobile. Hamburger button only visible on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add touch event isolation to TimelineControl and verify all panels</name>
  <files>
    src/components/ui/TimelineControl.jsx
  </files>
  <action>
    **TimelineControl.jsx — Touch isolation:**
    - Add touch event isolation handlers to the root div: `onTouchStart={e => e.stopPropagation()}`, `onTouchMove={e => e.stopPropagation()}`, `onPointerDown={e => e.stopPropagation()}`.
    - This prevents dragging the speed slider or tapping play/pause from rotating the 3D scene.
    - On mobile, adjust positioning if needed to avoid overlapping the bottom sheet InfoPanel. The timeline is at `bottom-4` which is above a collapsed InfoPanel. When the InfoPanel bottom sheet is open (60vh), the timeline may be obscured — this is acceptable because the user is reading planet data, not adjusting speed. No change needed.
  </action>
  <verify>
    Run `npm run build` — zero errors.
    In Chrome DevTools mobile simulation:
    - Touch-drag on the speed slider — scene does NOT rotate.
    - Touch-drag on the NavSidebar — scene does NOT rotate.
    - Touch-drag on the InfoPanel bottom sheet — scene does NOT rotate.
    - Touch outside all panels on the 3D canvas — scene rotates normally via OrbitControls.
  </verify>
  <done>
    All three interactive panels (NavSidebar, InfoPanel, TimelineControl) have stopPropagation on onTouchStart, onTouchMove, and onPointerDown. Touch interactions on panels are fully isolated from the 3D scene.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete responsive UI overlay: NavSidebar (desktop pinned, mobile hamburger), InfoPanel (desktop right sidebar, mobile bottom sheet), TimelineControl with touch isolation, CompositionChart animated bars, AU Brussel footer badge.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5174 in Chrome
    2. **Desktop verification (full-width browser):**
       - NavSidebar should be pinned on the left listing Sun + 9 planets
       - Click any planet in sidebar — camera flies to it, sidebar shows active highlight
       - InfoPanel should slide in from the right with stats, fun facts, moons, and animated composition bar chart
       - Speed slider at bottom center — drag it to change animation speed
       - "Created by AU Brussel" badge visible at bottom-right
       - No hamburger button visible
    3. **Mobile verification (Chrome DevTools → Toggle device toolbar → iPhone 14 or 375px width):**
       - Hamburger button visible at top-left (no sidebar visible)
       - Tap hamburger — sidebar slides in from left
       - Tap a planet in sidebar — sidebar closes, camera flies to planet
       - InfoPanel appears as bottom sheet from below (not from right)
       - Bottom sheet has rounded top corners and drag handle indicator
       - Speed slider visible at bottom (when InfoPanel is closed)
    4. **Touch isolation (in DevTools mobile simulation):**
       - Touch-drag on sidebar/InfoPanel/speed slider — 3D scene does NOT rotate
       - Touch-drag on empty canvas area — scene rotates normally
    5. **Composition chart:**
       - Select any planet — bars should sweep from 0% to final width
       - Switch to a different planet — bars re-animate
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — zero errors
2. Desktop (>=768px): sidebar pinned left, InfoPanel right-slide, no hamburger
3. Mobile (<768px): hamburger visible, sidebar slides from left, InfoPanel is bottom sheet
4. Touch isolation: panel drag does not rotate 3D scene on any panel
5. All Phase 3 functionality still works (click planet, keyboard 1-9, Escape, Space)
6. CompositionChart animates bars on every planet switch
7. Speed slider dual-dispatches to both stores
8. Footer badge visible at all times
</verification>

<success_criteria>
- Desktop layout: sidebar pinned left + InfoPanel slides from right
- Mobile layout: hamburger nav + bottom sheet InfoPanel
- Hamburger button only visible on mobile (md:hidden)
- Sidebar auto-closes on planet selection (mobile)
- Touch events on all HTML panels do not rotate the 3D scene
- All existing Phase 3 functionality preserved (fly-to, keyboard shortcuts, hover effects)
- npm run build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-and-responsive/04-02-SUMMARY.md`
</output>
